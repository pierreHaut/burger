{% extends 'base.html.twig' %}

{% block title %}Edit Burger{% endblock %}

{% block body %}
    <h1>Editer Burger</h1>

    {{ include('burger/_form.html.twig', {'button_label': 'Update'}) }}
    <label>Supprimer </label>{{ include('burger/_delete_form.html.twig') }}
    <a href="{{ path('burger_index') }}" class="btn btn-primary">Retour à la liste</a>

  
{% endblock %}
